import React from "react";
import Router from "../../hoc/Router/Router";

const Content = props => {
  return (
    <div>
      <h4>Content</h4>
      <Router />
    </div>
  );
};

export default Content;
